<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.inkapplications.aprs.android">
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/AprsTheme.Compat.DayNight"
        android:name=".AprsApplication">
        <activity
            android:name=".startup.StartupActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".capture.CaptureActivity" />
        <activity android:name=".station.StationActivity" />
        <activity android:name=".settings.SettingsActivity" />
    </application>
</manifest>
